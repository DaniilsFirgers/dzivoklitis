version: '3.8'

services:
  flats-parser:
    image: daniilsfirgers/flats-parser:python
    volumes:
      - ./config.toml:/app/config.toml 
      - ./flats.json:/app/flats.json  
    command: python3.10 -u /app/scraper/main.py